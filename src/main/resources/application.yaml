spring:
  application:
    name: franchises-api
  
  datasource:
    url: jdbc:mysql://localhost:3306/franchises_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: franchise_user
    password: jCR7D53n&3]z
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8081

  